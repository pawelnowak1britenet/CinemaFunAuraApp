<aura:component description="Result"
                implements="flexipage:availableForAllPageTypes,flexipage:availableForRecordHome,force:hasRecordId"
                access="global">

    <aura:attribute name="item" type="Object"/>

    <!--    //czy przekazywać atrybutem czy z eventu ?? REFACTOR -->
    <aura:attribute name="searchMovies" type="Boolean"/>

    <lightning:overlayLibrary aura:id="overlayLib"/>
    <div class="slds-box slds-col" onclick="{! c.showModal }">
            <aura:if isTrue="{! equals(v.searchMovies, true) }">
<!--                <h2>{!v.item.original_title}</h2>-->
                <aura:if isTrue="{!v.item.backdrop_path != null}">
                    <img src="{!'https://www.themoviedb.org/t/p/w220_and_h330_face' + v.item.backdrop_path}"/>
                </aura:if>
                <p>{!v.item.popularity}</p>
                <p>{!v.item.vote_average}</p>
            </aura:if>

            <aura:if isTrue="{! equals(v.searchMovies, false) }">
                <h2>{!v.item.name}</h2>
                <aura:if isTrue="{!v.item.profile_path != null}">
                    <img src="{!'https://www.themoviedb.org/t/p/w300_and_h450_bestv2' + v.item.profile_path}"/>
                </aura:if>
                <p>{!v.item.popularity}</p>
            </aura:if>


<!--        <lightning:button class="show" variant="brand" label="Pokaż szczegóły" title="Brand action"-->
<!--                          onclick="{! c.showModal }"/>-->
    </div>
</aura:component>